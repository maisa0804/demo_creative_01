extends ../base.pug

block vars
  - var template = 'home'

block content
  .home 
    .home__wrapper 
      each media in home.data.gallery 
        figure.home__gallery__image 
        
        a(href='/collections')=home.data.button